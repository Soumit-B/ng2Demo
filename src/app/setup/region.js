export var Region = (function () {
    function Region(code, name, locale_key) {
        this.code = code;
        this.name = name;
        this.locale_key = locale_key;
    }
    return Region;
}());
